[cols="1,2", options="header"]
|===
| Item | Value

| Hub OpenShift Console
| {openshift_console_url}

| Hub API Endpoint
| {openshift_api_url}

| Hub Web Login
| Username: `{user}` / Password: `{password}`

| Hub Bastion SSH
| `{hub_ssh_command}` (password: `{bastion_ssh_password}`)

| Grafana (Observability)
| {grafana_url} (log in via OpenShift OAuth with Hub credentials)

| Student-1 Console
| {student_1_console_url}

| Student-1 Web Login
| Username: `admin` / Password: `{student_1_admin_password}`

| Student-1 API Endpoint
| {student_1_api_url}

| Student-1 Bastion SSH
| `{student_1_ssh_command}` (password: `{student_1_bastion_ssh_password}`)
|===
