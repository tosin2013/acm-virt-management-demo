apiVersion: v1
kind: Service
metadata:
  name: nginx-subscription-vm
  namespace: vm-subscription-demo
  labels:
    app: nginx-subscription
spec:
  selector:
    app: nginx-subscription
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
